{
  "name": "uploading_ozon_orders_to_the_database",
  "version": "1.0.0",
  "description": "Ozon orders sync to PostgreSQL",
  "main": "src/app.js",
  "type": "module",
  "scripts": {
    "start": "node src/app.js",
    "init-db": "node -e \"import('./src/database.js').then(m => m.initDatabase())\""
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
